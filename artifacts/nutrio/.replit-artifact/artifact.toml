kind = "web"
previewPath = "/"
title = "Nutrio"
version = "1.0.0"
id = "artifacts/nutrio"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18653

[services.development]
run = "pnpm --filter @workspace/nutrio run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/nutrio", "run", "build" ]
publicDir = "artifacts/nutrio/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18653"
BASE_PATH = "/"
